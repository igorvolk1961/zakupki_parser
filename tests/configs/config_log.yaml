# Тестовый конфиг логирования: лог только в консоль, файл не создаём.
level: "INFO"
format: "%(asctime)s %(levelname)-8s [%(name)s] %(message)s"
file: null
file_level: "DEBUG"
console: true
truncate_on_start: true
