# Этап 0. Базовая линия и документация — детальный план

> Источник: мастер-план `.kilo/plans/1787250023996-architecture-multitenancy-master-plan.md`
> (Этап 0), трекер `plans/plan.md`.
> Характер этапа: **документационный** — поведение приложения не меняется, исходный
> код (`src/`) не затрагивается. Только `docs/`, `plans/`, `README.md`, `specification.md`.

## Цель

Зафиксировать зелёную тестовую базу до начала архитектурных изменений, зафиксировать
требования `docs/system_analisys/` в репозитории и описать целевую архитектуру
(TenderSearch) в `specification.md`/`README.md` как roadmap для этапов 1–10.

## Контекст

- `docs/system_analisys/` уже закоммичен: коммит `2c17529` (задача 0.1 — **выполнена**).
- Мастер-план с решениями R1–R9 лежит локально (`.kilo/` gitignored) — ссылаемся на него,
  не дублируем.
- Текущая база тестов: unit 277 passed; integration 14 passed / 70 skipped (без DSN).

## Задачи (по порядку)

### 0.1. Зафиксировать `docs/system_analisys/` в git — ✅ ВЫПОЛНЕНО (коммит `2c17529`)
- [x] Проверить состав коммита: `git show --stat 2c17529` (13 файлов docs + `plans/plan.md`).
- [x] Отметить задачу ✅ в `plans/plan.md` (Этап 0).

### 0.2. Базовая линия тестов и линтеров (до правок документации)
- [ ] Прогнать и зафиксировать результаты:
  - `uv run pytest tests/unit -q` → ожидание **277 passed**;
  - integration при доступном DSN: `ZAKUPKI_TEST_DSN='postgresql+asyncpg://...' uv run pytest tests/integration -q`;
    без DSN — зафиксировать **14 passed / 70 skipped** (не блокирует);
  - `uv run ruff check src tests`;
  - `uv run ruff format --check src tests`;
  - `uv run mypy src tests`.
- [ ] Вписать результаты в `plans/plan.md` (Этап 0, задача «прогон базовой линии»).

### 0.3. Gap-анализ «текущее состояние vs docs/system_analisys»
- [ ] Составить таблицу покрытия: требование docs → статус (реализовано/частично/отсутствует)
      → место в коде → этап, который закрывает.
- [ ] Охватить: Эпики 1–9 (US), BR-01…BR-07, NFR (COST/PERF/SEC/REL/OBS/FT), ER-модель,
      терминологию (User/Profile/Keyword/Evaluation/Subscription/AuditLog/Category).
- [ ] Результат ложится в `specification.md` (раздел ниже) и используется как вход для
      детальных планов этапов 1–10.

### 0.4. `specification.md` — раздел «Целевая архитектура: TenderSearch»
- [ ] Добавить раздел «Целевая архитектура (TenderSearch)»:
  - ссылка на мастер-план (`plans/00_plan.md` → `.kilo/plans/…-master-plan.md`);
  - целевая модель данных (эволюция существующей: `users`+status/email/trial_end_date,
    `client_profiles`→`profiles`+user_id, `procurement_scores`→`procurement_evaluations`+user_id,
    новые `keywords`/`audit_log`/`subscriptions`/`procedure_categories`);
  - краткая сводка решений R1–R9;
  - roadmap этапов 0–10 с привязкой к трекеру `plans/plan.md`.
- [ ] Существующие разделы спецификации пометить как «текущее состояние (v0.3.0)» —
      **не переписывать** до соответствующих этапов.

### 0.5. `README.md` — дорожная карта
- [ ] Добавить секцию «Дорожная карта: перестройка в TenderSearch»: ссылки на мастер-план,
      трекер `plans/plan.md` и `plans/00_plan.md`; краткий перечень этапов 0–10.

### 0.6. Финальная проверка этапа
- [ ] Повторный прогон 0.2 (тесты/линтеры зелёные — поведение не изменилось).
- [ ] `uv run zp --configs configs check-config` — конфиги валидны.
- [ ] `git diff --stat` — только документация (`specification.md`, `README.md`, `plans/*`).
- [ ] Коммит (по запросу) с сообщением в стиле репозитория, например:
      «docs: целевая архитектура TenderSearch, gap-анализ, roadmap этапов (Этап 0)».

## Вне скоупа (не делаем на этапе 0)

- Изменения исходного кода `src/` (миграции, репозиторий, API, парсер) — это этапы 1+.
- Запуск/остановка парсера, БД, инфраструктуры.
- Переписывание актуальных разделов `specification.md` под целевую модель.

## Заглушки

- Нет: этап не изменяет поведение приложения.

## Валидация

- `pytest` unit 277 + integration без регрессий;
- `ruff check`, `ruff format --check`, `mypy` — без ошибок;
- `zp check-config` — валиден;
- `git diff --stat` показывает только изменения документации.

## Критерии приёмки

1. `docs/system_analisys/` в git (✅ коммит `2c17529`).
2. Тестовая база зафиксирована зелёной (результаты в `plans/plan.md`).
3. `specification.md` содержит раздел целевой архитектуры и gap-анализ.
4. `README.md` содержит roadmap этапов.
5. Поведение приложения не изменилось (нет изменений исходного кода).

## Риски

- **Интеграционные тесты требуют БД/DSN**: если окружение без БД — фиксируем статус
  «skipped» и не блокируем этап; полный прогон integration — при доступном DSN.
- **Соблазн переписать спецификацию под целевую модель заранее**: сознательно
  добавляем только целевой раздел, текущие разделы правим на соответствующих этапах,
  чтобы документация не опережала код.

## Открытые вопросы

- Нет блокирующих. Минимальный объём gap-анализа: таблица покрытия (требование →
  статус → этап) + целевой раздел в спецификации.
