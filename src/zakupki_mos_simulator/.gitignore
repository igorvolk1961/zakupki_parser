# Root .gitignore содержит глобальный паттерн `data/` — а наш каталог data/ является
# Python-модулем подпроекта. Переопределяем правило, чтобы модуль попал в git.
!data/
!data/*
!data/**/*.py

# Рантайм-артефакты подпроекта — не коммитим.
data/parser.log
.ruff_cache/
