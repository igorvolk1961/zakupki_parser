"use strict";

// Общее изменяемое состояние приложения, видимое нескольким модулям.
// Локальное состояние каждого экрана живёт в своём модуле; сюда вынесено
// только то, что читают/меняют разные домены.
export const state = {
  authUser: null, // профиль пользователя из /api/auth/me (null — без авторизации)
  authRequired: false, // авторизация включена на сервере (нужен токен для WS)
  wsSocket: null, // текущий WebSocket канала обновлений
  parserRunning: false, // парсер запущен (влияет на таблицу закупок и кнопки)
};
