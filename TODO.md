# TODO

Список незавершённых работ и направлений развития.

## Ближайшие задачи
- [x] **Параллельная обработка площадок (R5, Этап 4B)** — `Scheduler.run_once`
      обрабатывает включённые площадки параллельными asyncio-задачами с лимитом
      `config_parser.yaml -> max_concurrent_platforms` (дефолт 2, 1 — последовательно);
      опция `profile_then_platform` удалена (ADR-11).
- [ ] **Кэш ЭТП (R4, Этап 4A)** — перенесён в пост-MVP: в дефолтном `platform_then_profile`
      одинаковые обходы уже дедуплицируются за цикл, а TTL `0.5×period` < period; ценность —
      при повторе запросов между воркерами (4C/10), где и реализуем кэш (ADR-11).
- [x] **Авто-пуш задания на скоринг в транспорт (ADR-7, шаг 1)** — после успешного
      сохранения новой закупки парсер отправляет `POST /api/scoring/jobs
      {procurement_id, priority, stage="fit", profile_id}` в `scoring_transport`
      (приоритет — время обновления/публикации закупки; `ScoringTransportClient`
      в `scoring.py`, `orchestrator.py`).
- [x] **Порог `notify_min_fit_score` + отложенное уведомление (ADR-7, шаг 6)** — в обработчике
      `POST /api/procurements/{id}/score` после обновления `fit_score` сравнивается
      `fit_score ≥ notify_min_fit_score` и подписчики уведомляются только при прохождении порога;
      безусловное уведомление из цикла парсинга (`orchestrator.py`) убрано.
- [x] **Выпил deprecated-путей скоринга (ADR-7)** — удалены `ExternalScoreClient`
      (`before_save`/`worker`), `Scheduler.run_scoring_worker`, `zp score-worker`,
      `list_for_scoring`/`set_score_method` и дублирующая fit-таблица `scoring_transport`
      (приоритет передаётся из парсера).
- [x] **Синхронизация документации с ADR-7** — обновлены `docs/c4/*.md`, `specification.md`,
      `plans/plan.md`, `README.md` под канонический конвейер скоринга (транспорт + Redis +
      сервис скоринга, авто-пуш, пороговое уведомление).
- [x] **Синхронизация документации с каскадом Fit→P(win)→Margin (ADR-9)** — README,
      `specification.md`, `docs/c4/*.md`, `docs/adr.md` (ADR-9), `plans/plan.md`,
      `docs/user-guide.md`, `docs/external-service-contract.md` обновлены под каскад:
      стадии `pwin_service`/`margin_service`, переходы по порогам, постадийные уведомления.
- [x] **Эндпоинт чистки БД** — реализован `POST /api/db/clear` (полная очистка закупок и
      заказчиков, доступен при остановленном парсере; web-интерфейс).
- [x] **Авторизация и роли в web-приложении** — авторизация всегда включена (вход/
      регистрация); роли `user`/`admin`/`analyst`/`devops` определяют набор вкладок;
      admin управляет пользователями (создание, роли, блокировка, удаление);
      админ-действия (управление парсером, очистка БД) и правка конфигурации защищены
      ролями `devops`/`analyst`.
- [ ] **`kpgz_codes`/`advance` на детальных страницах ЕИС** — дополнительные поля карточки
      закупки (обеспечение/аванс уже сделаны на ЕИС, КПГЗ и аванс — осталось).
- [ ] **`DELETE /api/procurements` по фильтрам/возрасту записи** (сейчас доступна только
      полная очистка `POST /api/db/clear`); при удалении записей удалять и связанные файлы
      из хранилища (S3/local).
- [ ] **Ранний пропуск прохода по числу результатов поиска** — реализован движок
      (`list_config.total_results_selector` + `total_results_regex`,
      `parser/lister.py:extract_total_results`, `repository.count`, ранний `return` в
      `run()` для relevance-режима). Осталось: верифицировать
      селектор счётчика для каждой площадки (`zp capture-fixture`) и включить в
      `configs/dom/<platform_id>.yaml` (у zakupki.mos.ru — закомментирован).
- [ ] **Верифицировать DOM-шаги фильтров** (`config_dom.yaml -> filters`) для площадок
      с панелью фильтров (для zakupki.mos.ru реализован и проверен URL-фильтр
      `search`, DOM-шаги не нужны).
- [ ] **`fetch-tree` — автоматический сбор маппинга ОКПД2 (код → путь)**:
      открыть дерево ОКПД2 площадки, развернуть все узлы, снять `value` чекбоксов
      и подписи (код в скобках), сохранить `data/okpd2_tree.json`; конфиг держит
      коды, парсер резолвит их в `okpdPaths` при старте. Сейчас маппинг собран
      вручную (снимок `configs/codes/okpd2_.html` → `configs/codes/mos_okpd2_tree.json`,
      коды 62/63/01 и др., резолв уже реализован).
- [ ] **Тест с имитацией сбоя БД**: проверить поведение `_persist` при транзиентной
      ошибке (retry с backoff, затем открытие circuit breaker) и при ошибке данных
      (запись пропускается, circuit breaker НЕ открывается). Имитация — мок репозитория
      (поднимающий транзиентные/data-исключения) или остановка контейнера PostgreSQL.
- [ ] **Повысить покрытие тестами** (сейчас ~64% строк при полном прогоне с БД;
      без БД — 57%). Приоритеты:
  - **Оркестратор против локального HTTP-сервера** (см. ниже) — главный разрыв:
    `parser/orchestrator.py` сейчас 28% (основной цикл, `_persist`/ретраи/CB, скоринг);
  - unit-тесты `parser/filters.py` (26%) — движок DOM-шагов фильтров;
  - smoke-тесты entry-points с 0%: `cli.py` (`check-config`/`stop`),
    `scheduler.py`, `browser/manager.py`, `browser/stealth.py`.
  Для замера: `ZAKUPKI_TEST_DSN=… python -m coverage run --source=zakupki_parser -m pytest`.
- [ ] Пересоздание `uv.lock`.

## Развитие
- [ ] **ЕИС (zakupki.gov.ru)** — уточнить:
  - файлы 223-ФЗ (путь `notice223/documents.html?purchaseNoticeNumber=…` — отличается от 44-ФЗ);
  - детальные поля `common-info.html` по типам извещений (ezt20/ea20/zk20/ok504…);
  - `kpgz_codes`/`advance` на детальных страницах;
  - устойчивость: таймаут детальной страницы не должен прерывать всю площадку.
- [ ] **Блокирующее потребление задач** — заменить поллинг `ZPOPMAX` в сервисах стадий
      (`scoring_service`/`pwin_service`/`margin_service`) на `BZPOPMAX` (Redis 6.2+)
      для мгновенного «пробуждения» при появлении задания (ADR-7). Подробности — в `docs/adr.md` (ADR-7).
- [ ] **Калибровка скоринга** — подобрать пороги regression-гейта и влияние ветки
      Giga-эмбеддингов (`giga_embedding_alpha`) по метрикам `evaluate`; проставить рейтинги
      заказчиков (`customers.rating`) и включить их в `P(win)`.
- [ ] **Калибровка каскада Fit→P(win)→Margin (ADR-9)** — коэффициенты формулы P(win)
      (`base_pwin`, `k_smp`, `k_license`, `k_large`, `k_procedure`, `k_ai`) и пороги
      постадийных уведомлений (`notify_min_fit_score`/`notify_min_pwin`/
      `notify_min_margin`) по накопленным данным; после калибровки снять заглушку
      P(win) (`pwin_service` config `use_stub: true` → `false`). (Пороги переходов
      `pwin_fit_threshold`/`margin_pwin_threshold` удалены с отключением автокаскада,
      ADR-10.)
- [x] **Добавить коммерческие ЭТП (движок + конфиги-каркасы)** — расширены механизмы движка
      (URL-пагинация `page_param`/`page_size`, вложенные array-параметры); добавлены конфиги
      Росэлторг (44/223), Фабрикант,
      B2B-Center, ЭТП ГПБ (44/223), lot-online (44/223) в `config_dom.yaml` + список в
      `config_service.yaml` (все `enabled: false`). План: `.kilo/plans/1786484077646-commercial-etps-addition-plan.md`.
- [x] **Верификация селекторов ЭТП ГПБ (etpgpb)** — снимки списка/детали/организации
      (2026-08-14): детальные переменные (статус/заказчик/ОКПД2 из «Классификатор ОКПД2»), файлы
      (Документация), ИНН со страницы организации (точный `inn_page_selector`), исправлен `subject`
      (`div.productCard__title a`). Площадка не различает 44/223 — одна площадка `etpgpb`
      (etpgpb_223 удалён, etpgpb_44 переименован в etpgpb).
- [x] **etpgpb: сбор списка через JSON API** (2026-08-18) — SSR-страница `/procedures/` всегда
      рендерит базовую выдачу, фильтрует только внутренний API `/api/v2/procedures/` (SPA дергает
      его после гидрации). Реализован API-листер (`search.api_endpoint`): список из JSON, детали/
      файлы/ИНН — DOM. Выяснено и исправлено:
      - `procedure[name]` не фильтрует; реальный текстовый поиск — `search`, работает **только** с
        `sort=by_relevance` (с `by_published_desc` API возвращает нерелевантные записи);
      - индексированная форма `procedure[okpd][0]=...` API игнорирует (возвращался весь каталог) —
        работает плоская `procedure[okpd]=<код>` (префиксный матчинг, несколько кодов через запятую);
      - короткие слова (1–2 символа) движок перематчивает в подстрочный шум (`search=ИИ` → 63
        нерелевантные закупки) — задан `min_keyword_len: 3`, как на fabrikant.
- [ ] **Верификация селекторов прочих коммерческих ЭТП** — для каждой площадки `zp capture-fixture`
      (список + детали), заполнить реальные селекторы/параметры ОКПД2 и слов вместо плейсхолдеров,
      затем включить (`enabled: true`) по одной и прогнать `zp run-once`.
- [ ] **fabrikant: ОКПД2 и файлы на деталях (client-rendered)** — карточки списка и базовая часть
      деталей серверно-рендерятся (data-slot-селекторы в `configs/dom/fabrikant.yaml` проверены),
      но разделы «Код по ОКПД2» и «Документы по закупке» догружаются клиентом. Проверить на живой
      странице (`zp capture-fixture`/`zp run-once`): подобрать селекторы `okpd2_code`/`okpd2_name`
      и `detail.files` (сейчас `a[href*='zakupki.gov.ru/{44fz,223fz}/filestore']`).
- [ ] **fabrikant: полный маппинг ОКПД2 `code_to_id`** — фильтр по ОКПД2 работает через внутренние
      opaque-id площадки (`okpd2[]`); маппинг в `configs/codes/fabrikant_okpd2_tree.json` частичный
      (62, 62.01–62.09). Собрать полное дерево (`fetch-tree`) и проверить резолв.
      ✅ Панель «Расширенный поиск» подключена (2026-08-24): ОКПД2 -> `okpd2[]` (bracket-формат),
      НМЦ -> `price_from`/`price_to` (см. `configs/dom/fabrikant.yaml`).
- [ ] **fabrikant: детальные страницы коммерческих типов** — `v2/trades/procedure/`,
      `trades/atom/PriceRequest|ProposalRequest|PriceMonitoring` имеют иную разметку (без
      `field-label`/`field-text`); извлечение nmck/customer/inn/status полноценно только для 44-ФЗ
      (`44.fabrikant.ru`). Добавить селекторы для коммерческих деталей.
- [ ] Ротация прокси и пул IP в `config_parser.yaml` (при появлении инфраструктуры).
- [ ] Авторизация (личный кабинет) для закрытых разделов.
- [ ] Загрузка отчётов в XLSX/CSV по требованию (формат для заказчиков).
- [ ] Метрики (кол-во собранных/пропущенных/дубликатов за цикл) и healthcheck.
- [ ] Тесты на полный оркестратор против локального HTTP-сервера с фикстурами.
