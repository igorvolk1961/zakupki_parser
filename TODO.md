# TODO

Список незавершённых работ и направлений развития.

## Ближайшие задачи
- [ ] **Webhook**: заменить лог-заглушку реальной HTTP POST-отправкой JSON-карточки
      закупки (`config_service.yaml -> webhook.url/token`).
- [ ] **Обработка скачанных файлов** (извлечение переменных из PDF/DOCX) — выполняется
      внешним сервисом, в парсере не реализуется.
- [ ] **Верифицировать DOM-шаги фильтров** (`config_dom.yaml -> filters`) для площадок
      с панелью фильтров (для zakupki.mos.ru реализован и проверен URL-фильтр
      `search`, DOM-шаги не нужны).
- [ ] **`fetch-tree` — автоматический сбор маппинга ОКПД2 (код → путь)**:
      открыть дерево ОКПД2 площадки, развернуть все узлы, снять `value` чекбоксов
      и подписи (код в скобках), сохранить `data/okpd2_tree.json`; конфиг держит
      коды, парсер резолвит их в `okpdPaths` при старте. Сейчас маппинг собран
      вручную (снимок `docs/codes/odkp2_.html` → `docs/codes/okpd2_tree.json`,
      коды 62/63/01 и др., резолв уже реализован).
- [ ] **Тест с имитацией сбоя БД**: проверить поведение `_persist` при транзиентной
      ошибке (retry с backoff, затем открытие circuit breaker) и при ошибке данных
      (запись пропускается, circuit breaker НЕ открывается). Имитация — мок репозитория
      (поднимающий транзиентные/data-исключения) или остановка контейнера PostgreSQL.
- [ ] Автообновление `data/last_seen.json` + пересоздание `uv.lock`.

## Выполнено (проверено на живом сайте)
- [x] Селекторы карточки списка Реестра закупок `/purchase/list` (номер, тип, статус,
      предмет, заказчик, НМЦК, регион, закон, строка дат «с … до …»).
- [x] Селекторы значений детальной страницы `/need/{id}` (заказчик, НМЦК, ОКПД2) и
      элементов файлов `FileStorage/Download`.
- [x] Сортировка по дате публикации и пагинация.

## Развитие
- [x] **ОКПД2-фильтр ЕИС**: сервер требует полный список id/кодов поддерева
      (`okpd2Ids` + `okpd2IdsCodes`), корневой id игнорируется. Маппинг
      «код → id» собран обходом `children.html` (62→8873937, 63→8873938;
      `docs/codes/gov_okpd2_tree.json`), подключено через `criteria_map`
      (`query_params`). Проверено на живом сайте: 687 записей тематики ИТ за 2 дня.
- [x] **Файлы закупок ЕИС**: извлекаются с отдельной страницы `documents.html`
      (`detail.files_page` + селектор `filestore/public/`), имя + URL в `files_json`.
      Проверено на живом сайте.
- [x] **223-ФЗ на ЕИС**: включён (`fz223=on`), карточки 223-ФЗ (11-значные номера),
      заказчик `view223`, дедлайн из «Окончание подачи заявок», предмет из «Объект закупки».
- [ ] **ЕИС (zakupki.gov.ru)** — уточнить:
  - файлы 223-ФЗ (путь `notice223/documents.html?purchaseNoticeNumber=…` — отличается от 44-ФЗ);
  - детальные поля `common-info.html` по типам извещений (ezt20/ea20/zk20/ok504…);
  - устойчивость: таймаут детальной страницы не должен прерывать всю площадку.
- [ ] Добавить коммерческие ЭТП (Роселторг, B2B-Center и др.) — записи в `config_dom.yaml`
      + список сайтов в `config_service.yaml`.
- [ ] Ротация прокси и пул IP в `config_parser.yaml` (при появлении инфраструктуры).
- [ ] Авторизация (личный кабинет) для закрытых разделов.
- [ ] Загрузка отчётов в XLSX/CSV по требованию (формат для заказчиков).
- [ ] Retry-обёртка операций браузера с экспоненциальным backoff (сейчас — только
      circuit breaker на уровне площадки).
- [ ] Метрики (кол-во собранных/пропущенных/дубликатов за цикл) и healthcheck.
- [ ] Тесты на полный оркестратор против локального HTTP-сервера с фикстурами.
