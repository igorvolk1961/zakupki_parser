# Функциональные требования (FR)

> Формализованные функциональные требования TenderSearch (проверяемые формулировки
> «Система должна …»). Связаны с US/BR (`01_user_stories.md`, `02_business_rules.md`),
> NFR (`04_Data_and_NFR/02_non_functional_requirements.md`) и этапами (`plans/plan.md`).
> Статусы: ✅ реализовано · 🟡 частично · ❌ отсутствует (актуализировано 2026-08-24).

## Группа 1. Профили фильтрации (Эпик 1)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-1.1 | Система должна хранить профили фильтрации per-user (CRUD: создание, просмотр, обновление, удаление) с изоляцией по `user_id` (BR-07). | Must | US-1.1–1.4, BR-07 | ✅ | 1, 2 |
| FR-1.2 | Система должна хранить ключевые слова и слова-исключения профиля в таблице `keywords` (канонический источник), поддерживая синтаксис `слов*`, `(фраза* фраза*)~N`, точные фразы. | Must | US-1.1, US-1.3, BR-02 | ✅ | 2 |
| FR-1.3 | У каждого пользователя должен быть ровно один активный профиль (`is_active`); переключение деактивирует остальные. | Must | US-1.4, BR-07 | ✅ | 2 |
| FR-1.4 | Система должна позволять сид профиля из `файл-сид профиля` (`zp seed-profile` / `POST /api/clients/seed`). | Should | US-1.1, R8 | ✅ | 2 |
| FR-1.5 | Система должна контролировать формат кодов ОКПД2 при сохранении профиля (2–9 цифр, разделители точка/дефис/пробел; нормализация и дедупликация) на сервере и в форме (теги с проверкой при добавлении). | Should | US-1.5 | ✅ | 2 |
| FR-1.6 | Система должна показывать пользователю понятное сообщение об ошибке сохранения профиля (detail сервера, включая требование компетенций при включённой опции LLM-скоринга) и не закрывать редактор при ошибке. | Should | US-1.6, BR-09 | ✅ | 2 |

## Группа 2. Парсинг и первичный скоринг (Эпик 2)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-2.1 | Система должна периодически собирать закупки с целевых ЭТП (ЕИС 44/223, Портал Москвы, коммерческие ЭТП) по кодам ОКПД2 активного профиля (+ обход «без кода» при наличии позитивных слов, R9). | Must | US-2.1, BR-01, R9 | ✅ | 3 |
| FR-2.2 | Система должна применять ключевые слова и слова-исключения клиентски, ДО записи в БД; закупка без совпадений или с исключением не сохраняется. | Must | US-2.4, BR-02, R9 | ✅ | 3 |
| FR-2.3 | Система должна фиксировать в оценке (`matched_keywords`) слова, по которым закупка прошла фильтр. | Should | US-2.4, R9 | ✅ | 3 |
| FR-2.4 | Система должна автоматически ставить задание внешнего скоринга Fit (auto-Fit) для каждой сохранённой закупки через `scoring_transport` (ADR-7) и сохранять результат в `procurement_evaluations` под активный профиль. | Must | US-2.2, BR-02 | ✅ | 3 |
| FR-2.5 | Система должна исключать дубликаты закупок по `(number, platform_id)` и пропускать неизменившиеся записи (BR-01). | Must | US-2.1, BR-01 | ✅ | 3 |
| FR-2.6 | Система должна возвращать список закупок, отсортированный по убыванию Fit, с фильтрами (active, min_fit_score, scored, okpd2, customer). | Must | US-2.3, NFR-PERF-1 | ✅ | 3 |
| FR-2.7 | Система должна удалять нерелевантные (`fit_score < порог`) и неактивные закупки по команде администратора (при остановленном парсере). | Should | US-2.5 (архив), BR-01 | ✅ | 3 |

## Группа 3. Доставка и экспорт (Эпик 3, 6)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-3.1 | Система должна отправлять постадийные уведомления (fit/pwin/margin) подписчикам (Telegram/MAX/Webhook), когда результат стадии прошёл её порог (`notify_min_fit_score`/`notify_min_pwin`/`notify_min_margin`). | Must | US-3.1, BR-02 | ✅ | 3 |
| FR-3.2 | Карточка закупки должна содержать базовые поля (номер, заказчик, закон, НМЦК, дедлайн, предмет, Fit, ссылка на ЭТП). | Must | US-3.2 | ✅ | 3 |
| FR-3.3 | Система должна экспортировать активные релевантные закупки в CSV (UTF-8 BOM) в `export_dir`. | Must | US-3.3 (XLSX — целевое) | ✅ CSV, 🟡 XLSX | 3, 8 |
| FR-3.4 | (Целевое) Система должна формировать XLSX-сводку «В работу» с маркерами проверок и дисклеймером (NFR-SEC-4). | Should | US-6.1, US-6.2 | ❌ | 8 |

## Группа 4. Детальная проверка ТЗ (Эпик 4)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-4.1 | Система должна выполнять on-demand анализ ТЗ (US-4.1) двухстадийно: **Stage A** — извлечение фактов из ТЗ (системные проверки — один batch-LLM-вызов по лексически отобранным секциям; пользовательские вопросы — эмбеддинги → top-k → LLM-вердикт), **Stage B** — сопоставление фактов ТЗ с фактами профиля детерминированными правилами → `rag_report`. Профиль в промпт не попадает. | Must | US-4.1 | ✅ | 5 |
| FR-4.2 | Система должна при недоступности LLM/файла ТЗ не ронять задание (best-effort, `tz_found=false` или пометка в вердикте). | Must | US-4.1, NFR-FT-2 | ✅ | 5 |
| FR-4.3 | Система должна выполнять обязательные системные проверки ТЗ (опыт ПП РФ 2571 BR-03, реестр Минпромторга с учётом «не установлено» BR-04, лицензии/СРО) для каждой закупки независимо от профиля: вопросы ids `sys:*`, версия `SYSTEM_QUESTIONS_VERSION`, маркеры 🔴/🟡/🟢 в карточке. | Must | US-4.2–4.5, BR-03, BR-04 | ✅ | 5 |
| FR-4.4 | Система должна защищать системные проверки от редактирования через профиль: `profiles.questions` не сохраняет вопросы с id `sys:*`; в форме профиля они отображаются read-only. | Must | US-4.2–4.5, BR-03, BR-04 | ✅ | 5 |
| FR-4.5 | Система должна сопоставлять факты ТЗ с фактами профиля в коде (Stage B, `analysis_service/pipeline/matcher.py`): опыт через площадку → 🔴 при отсутствии `platform` в фактах профиля, сканы/реестр → 🟡; лицензия → 🔴/🟢 по `license_codes`; нераспознанный вид лицензии → 🟡 «требует проверки» (recall-over-precision). | Must | US-4.2–4.5, BR-03, BR-04 | ✅ | 5 |
| FR-4.6 | Система должна отдавать конвейеру факты активного профиля (коды лицензий и типов подтверждения опыта) в `GET /api/clients/active` → `facts` (для Stage B). | Must | US-4.2–4.5, BR-03 | ✅ | 5 |

## Группа 5. Решения и обратная связь (Эпик 5)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-5.1 | Система должна позволять тендерологу ставить статус «В работу»/«Отклонить» с причиной и скрывать отклонённые из выдачи. «Отклонить» — статус `rejected` + `rejection_reason` в `procurement_evaluations` (per-profile); «В работу» — запись в `procurement_work_items` (per-profile, BR-07) с признаком `in_work` в выдаче. | Must | US-5.1, US-5.2, US-2.5 | ✅ | 7 |
| FR-5.2 | (Целевое, отложено) Система должна предлагать добавить слово/правило в профиль при отклонении (без автоприменения, без повторов предложения). | Could | US-5.3 | ❌ | пост-MVP |
| FR-5.3 | При отклонении закупки система должна давать тендерологу явные действия: убрать из профиля ключевые слова, по которым закупка отобрана (`matched_keywords`), и/или добавить слово-исключение в профиль (идемпотентно). | Should | US-5.2 | ✅ | 7 |
| FR-5.4 | Система должна позволять принять закупку «в работу» из результатов поиска (per-profile) и помечать её признаком `in_work` в выдаче. | Must | US-5.4, BR-07 | ✅ | 7 |
| FR-5.5 | Система должна позволять принять закупку «в работу» по URL ЭТП: при наличии закупки в `procurements` — привязать к ней, иначе — создать запись-снимок (`procurement_id=NULL`). | Should | US-5.5 | ✅ | 7 |
| FR-5.6 | Система должна вести раздел «В работе» активного профиля единым механизмом с выдачей (`GET /api/procurements?in_work=true` — тот же список и карточка/анализ), снимать закупку с «в работе» (удаляется только запись, закупка остаётся в общей выдаче); для закупок «в работе» операции «В работу»/«Отбраковать» недоступны. | Must | US-5.6, BR-07 | ✅ | 7 |
| FR-5.7 | При очистке БД (`POST /api/db/clear`) система должна запрашивать, удалять ли закупки «в работе»; по умолчанию записи сохраняются (`procurement_id` обнуляется FK `ON DELETE SET NULL`, карточка — из снимка в записи). | Should | US-5.4–5.6 | ✅ | 7 |

## Группа 6. Доступ, аккаунты, мультитенантность (Эпик 7)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-6.1 | Система должна поддерживать регистрацию и аутентификацию (логин/пароль, PBKDF2) с ролями `user`/`admin`/`analyst`/`devops`; доступ к API — через Bearer-токен. | Must | US-7.1, BR-07 | ✅ | 1, 6 |
| FR-6.2 | Система должна изолировать данные по `user_id` (профили) и `profile_id` (оценки) на уровне репозитория (BR-07); попытка доступа к чужому тенанту — 403. | Must | US-7.8, US-7.9, BR-07 | ✅ | 1 |
| FR-6.3 | Система должна вести триал-режим и аккаунты опций: self-registered пользователь получает trial_end_at = now()+14 суток (TRIAL_DEFAULT_DAYS), в триале все реализованные опции поиска и скоринга доступны; по окончании триала доступность платных операций определяется активным аккаунтом (по умолчанию — только бесплатные опции, BR-09). Заморозка/удаление по BR-05 — целевая модель пост-MVP. | Must | US-7.1–7.3, US-10.5–10.6, BR-05, BR-09 | 🟡 | 1, 6 |
| FR-6.4 | Система должна позволять администратору управлять пользователями и их аккаунтами: роли, блокировка/разблокировка, удаление; просмотр/создание/переключение аккаунтов-наборов опций пользователя, редактирование платных опций, выдача/продление/снятие триала (вкладка «Пользователи» → «Опции»). | Must | US-7.6, US-7.7, US-10.8 | 🟡 | 6, 10 |
| FR-6.5 | Система должна предоставлять пользователю личный кабинет: список аккаунтов (несколько, активен один), создание/переименование/удаление неактивных, переключение активного аккаунта, каталог опций с состояниями, смена пароля с проверкой текущего. | Must | US-10.1, US-10.3, US-10.4, BR-09 | ✅ | 6 |
| FR-6.6 | Система должна по умолчанию создавать аккаунт только с бесплатными опциями (поиск/сбор, «В работе», уведомления, экспорт); платные опции пользователь включает сам в активном аккаунте; отложенные опции (платное гео) недоступны для включения. | Must | US-10.2, US-10.5, BR-09 | ✅ | 6 |
| FR-6.7 | Система должна не выполнять платные операции, недоступные в текущем триале/активном аккаунте: on-demand анализ/P(win)/Margin возвращают понятный отказ; фоновый сбор и recovery-постановка очереди не ставят задачи скоринга для такого владельца; профиль без компетенций можно сохранить, если активный аккаунт не включает LLM-обработку компетенций. | Must | US-10.7, US-10.9, BR-09 | ✅ | 6 |

## Группа 7. Наблюдаемость и устойчивость (Эпик 8)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-7.1 | Система должна логировать 100% LLM-запросов в LangFuse (input/output tokens, latency, cost). | Must | US-8.1, NFR-OBS-1 | ✅ | — |
| FR-7.2 | (Целевое) Система должна отдавать метрики `GET /metrics` (обработано тендеров, ошибки, латентность, стоимость). | Must | US-8.1 | ❌ | 10 |
| FR-7.3 | (Целевое) Воркеры парсинга/скоринга должны быть stateless и масштабироваться горизонтально через очередь (R4/R5). | Must | US-8.3, BR-06 | 🟡 стадии stateless; парсер — 4C | 4C, 10 |

## Группа 8. Compliance (Эпик 9)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-8.1 | (Целевое) Система должна отображать дисклеймер об информационном характере скоринга в UI и подвале экспортов (NFR-SEC-4). | Must | US-9.1 | ❌ | 8, 9 |
| FR-8.2 | (Целевое) Парсер должен уважать `robots.txt` и использовать официальные API (NFR-SEC-5). | Must | US-9.2 | ❌ | 9 |
| FR-8.3 | (Целевое) Система должна маскировать/не сохранять ПДн физлиц из текстов ТЗ (152-ФЗ). | Should | US-9.3, NFR-SEC-3 | ❌ | 9 |
| FR-8.4 | (Целевое) Система должна логировать критические действия пользователей в `audit_log` (timestamp, user_id, action, resource_id, IP), хранение ≥ 1 года. | Should | US-9.4, NFR-OBS-2 | ❌ | 6, 9 |

## Группа 9. Скоринг-каскад (сквозные требования)

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-9.1 | Система должна поддерживать стадии каскада Fit → P(win) → Margin за абстракцией `scoring_common/queue.py`; автокаскад отключён, P(win)/Margin — on-demand (`POST /api/procurements/pwin-margin`). | Should | US-2.2, BR-02 | ✅ | 3 |
| FR-9.2 | (Целевое) Обработка должна быть идемпотентной (повторная доставка задачи не дублирует записи и не списывает токены дважды). | Must | NFR-FT-5 | ✅ | 1, 10 |
| FR-9.3 | (Целевое) Ошибочные задачи должны уходить в DLQ после 3 попыток (BR-06). | Must | BR-06, NFR-REL-2 | 🟡 retry/backoff есть; DLQ — этап 10 | 10 |

## Группа 10. Операционная работа и инструментальные возможности (операционные требования)

> Операционные требования порождены ролями из `01_Business_Context/02_stakeholders.md`
> (DevOps/SRE, Системный администратор, Аналитик-настройщик мониторинга) и формализуют
> реализованный инструментальный функционал, ранее не покрытый FR. Связь «требование ↔
> код ↔ тест ↔ стейкхолдер» — в `traceability/requirements-registry.yaml` (источник правды).

| ID | Требование | Приоритет | US/BR | Статус | Этап |
| :-- | :--------- | :-------- | :---- | :----- | :--- |
| FR-10.1 | Система должна разделять доступ по ролям `user`/`admin`/`analyst`/`devops`: набор вкладок web-интерфейса — объединение вкладок ролей пользователя (RBAC). Стейкхолдер: DevOps/SRE, Сист.администратор. | Must | FR-6.1, BR-07 | ✅ | 1, 6 |
| FR-10.2 | Система должна позволять администратору управлять пользователями: создание (роли admin/analyst/devops), смена ролей, блокировка/разблокировка, удаление; запрет удаления себя и последнего `admin` (вкладка «Пользователи»). Стейкхолдер: Сист.администратор. | Must | US-7.6, US-7.7 | ✅ | 6, 10 |
| FR-10.3 | Система должна предоставлять эксплуатационные конфиги через web-интерфейс (роль `devops`): `config_ops.yaml` (вкладка «Конфигурация»), `config_log.yaml` («Управление логами»), `config_parser.yaml` («Парсер»); форма + «Текстовый режим» с сырым YAML. Секреты через API не редактируются (только env). Стейкхолдер: DevOps/SRE. | Must | NFR-SEC-2 | ✅ | — |
| FR-10.4 | Система должна предоставлять просмотр/редактирование конфигов фоновых сервисов и их секретов `.env` (вкладка «Сервисы», роль `devops`): форма по схеме `src/<service>/config.yaml` + «Текстовый режим» (raw YAML) + модальное окно «Секреты (.env)». Стейкхолдер: DevOps/SRE. | Must | NFR-SEC-2 | ✅ | — |
| FR-10.5 | Система должна предоставлять просмотр и фильтрацию логов всех сервисов (вкладка «Логи», роль `devops`, `GET /api/logs/tail`): поиск, фильтр по уровню и дате, автообновление. Стейкхолдер: DevOps/SRE. | Must | US-8.1, NFR-OBS-1/3 | ✅ | 8, 10 |
| FR-10.6 | Система должна предоставлять аналитические настройки (роль `analyst`): параметры мониторинга (`config_service.yaml`, вкладка «Параметры мониторинга»), редактор промптов сервисов («Промпты», валидация JSON), справочные таблицы («Справочники»: типы лицензий, типы подтверждения опыта). Стейкхолдер: Аналитик (настройка мониторинга) / SME. | Must | FR-4.4, BR-03 | ✅ | 1, 5 |
| FR-10.7 | Система должна предоставлять панель управления парсером (роль `devops`): запуск/остановка/статус, очистка БД (полная/неактивные/нерелевантные с порогом fit-score) при остановленном парсере; живые обновления по WebSocket. Стейкхолдер: DevOps/SRE. | Must | US-2.5 | ✅ | 3, 4 |
| FR-10.8 | Система должна защищать служебные вызовы конвейера внутренним токеном `X-Internal-Token` (env `ZAKUPKI_INTERNAL_TOKEN`): `POST /api/procurements/{id}/score`, `GET /api/clients/active`, `GET /api/config/scoring`; недоступность токена — fail fast. Стейкхолдер: DevOps/SRE, Архитектор. | Must | BR-07, NFR-SEC-2 | ✅ | 1 |
